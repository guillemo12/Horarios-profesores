<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <!-- 1. ACELERADOR: Modo puro aleatorio, más rápido -->
    <environmentMode>NON_REPRODUCIBLE</environmentMode>

    <solutionClass>com.colegio.solver.HorarioSolution</solutionClass>
    <entityClass>com.colegio.solver.Leccion</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>com.colegio.solver.HorarioConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <termination>
        <spentLimit>30m</spentLimit>
        <unimprovedSpentLimit>5m</unimprovedSpentLimit>
        <!-- <bestScoreLimit>0hard/0soft</bestScoreLimit> -->
    </termination>

    <!-- FASE 1: Construcción del borrador inicial -->
   <!-- <constructionHeuristic />
    -->
    <!-- FASE 2: ACELERADOR con el algoritmo Late Acceptance -->
    <!-- <localSearch>
        <localSearchType>LATE_ACCEPTANCE</localSearchType>
    </localSearch> -->

</solver>